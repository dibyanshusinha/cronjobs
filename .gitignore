node_modules/
npm-debug.log*
coverage/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store

# Never committed: this edition's secrets live in GitHub Secrets, not files.
# Kept here defensively for local testing and for the future self-hosted mode.
.env
.env.*
!.env.example
*.pem
*.key

# Local scratch state if you point STATE_DIR at the repo during manual testing.
cron-state-checkout/
.local-state/

# Standalone runtime data, backups, and SQLite files stay local to a deployment.
data/
backups/
*.sqlite
*.sqlite-*
*.db
